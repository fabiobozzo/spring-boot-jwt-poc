logging:
  level:
    root: INFO
    fb.poc: DEBUG

spring:
  h2.console.enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:
    show-sql: true
    hibernate.ddl-auto: create-drop
    jpa.properties.hibernate.dialect: org.hibernate.dialect.H2Dialect

jwt.secret: extremelyhardtoguess